#include<stdio.h>
int main()
{
    int a,b;
    printf("enter two numbers\n",a,b);
    scanf("%d/n %d/n",&a,&b);
    if (a<50 && a<b)
    {
        printf("The statement is true :%d\n",a,b);
        
    }
    else

    
    prinf("The condition is false :%d\n",a,b);
    return 0;
}