#include<stdio.h>
int main()
{
    int  *p;
    char *q;
    float *s;

    
printf(" the size of integer:%d",sizeof(p));

printf(" the size of char:%d",sizeof(q));

printf(" the size of float:%d",sizeof(s));
}
